[project]
name = "supply-chain-autoagent"
version = "0.1.0"
description = "AutoAgent-style harness for supply-chain order cut optimisation"
requires-python = ">=3.12"
dependencies = [
    "openai-agents",
    "pandas",
    "openpyxl",
    "numpy",
    "matplotlib",
    "harbor",
]

